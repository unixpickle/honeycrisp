# ``Tensor/checkNaN(onForward:onGrad:function:file:line:)``

Check the tensor for NaN (Not a Number) values during the forward and/or backward passes, and print the provided strings when NaNs are detected.