# ``Tensor/argmax(axis:keepdims:function:file:line:)``

Return the indices of the maximum values along the specified axis of the `Tensor`. If `keepdims` is `true`, then keep the given axis with a value of `1`.