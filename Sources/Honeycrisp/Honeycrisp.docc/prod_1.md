# ``Tensor/prod(axis:keepdims:function:file:line:)``

Compute the product of the elements in the ``Tensor`` along the specified axis, with an option to retain the axis of reduction with a value of `1`.
